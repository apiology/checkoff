# shellcheck shell=bash
PATH_add bin

direnv_load op run --cache --env-file=config/env.1p -- direnv dump
# Optional: 1Password Environment → Destinations → Local .env file at config/env.local
dotenv_if_exists config/env.local
